---
name: design-package2
description: Generate a comprehensive, team-vetted design package for the Option D home theater — DAG-parallel version with structural validation gates
---

# Design Package 2 — Option D Home Theater (DAG Execution)

Generate the Option D home theater design package. Output to `theater/team_test_<N>/` where N is the next available increment (check existing `team_test*` folders, never read them as source).

**This command is a task DAG.** The main conversation manages phase sequencing and the decision gate. Each wave below is dispatched as parallel agents up to the 8-agent cap. Do NOT skip phases or merge waves to save time.

---

## Pre-Run: Determine Output Directory

Before creating any tasks, determine N: `ls -d D:/Projects/Claude/theater/team_test*/` and use the next increment. All tasks write to `D:/Projects/Claude/theater/team_test_<N>/`. Record this path as OUT_DIR.

---

## DAG Summary Table

Present this table to the user when starting:

| ID | Task | Agent | Blocked By | Priority |
|----|------|-------|------------|----------|
| W0-A | Read POR snapshot + extract summary | build | — | high |
| W0-B | Read investigations + OBJ skill | build | — | high |
| W0-C | Generate coordinates.json | build | — | high |
| W0-D | Generate ts_params.json | build | — | high |
| W1-A | Architect audit | dp-architect | W0-A, W0-B, W0-C, W0-D | high |
| W1-B | Analyst baseline | dp-analyst | W0-A, W0-B, W0-C, W0-D | high |
| W2 | Critic review | dp-critic | W1-A, W1-B | high |
| W3-GATE | Decision gate (user) | gate | W2 | high |
| W4-0 | Generate specs.yaml | build | W3-GATE | high |
| W4-B1 | Write 00_index.md | build | W4-0 | high |
| W4-B2 | Write 01_room_electrical_hvac.md | build | W4-0 | high |
| W4-B3 | Write 02_speakers.md | build | W4-0 | high |
| W4-B4 | Write 03_subwoofers.md | build | W4-0 | high |
| W4-B5 | Write 04_amplification_dsp.md | build | W4-0 | high |
| W4-B6 | Write 05_screen_seating.md | build | W4-0 | medium |
| W4-B7 | Write 06_budget.md | build | W4-0 | high |
| W4-C0 | Generate diagrams/config.py | build | W4-0 | high |
| W5-C1 | Generate d01_floor_plan.py | build | W4-C0 | medium |
| W5-C2 | Generate d02_signal_flow.py | build | W4-C0 | medium |
| W5-C3 | Generate d03_front_elevation.py | build | W4-C0 | medium |
| W5-C4 | Generate d04_wiring.py | build | W4-C0 | medium |
| W5-D1 | Generate generate_room.py + room_model.obj | build | W4-0, W0-C | high |
| W5-D2 | Generate generate_room_hires.py + room_model_hires.obj | build | W4-0, W0-C | medium |
| W5-D3 | Generate generate_sub_cabinet.py + sub_cabinet.obj | build | W4-0, W0-C | medium |
| W6-VA | Run validate_geometry.py assertions | build | W5-D1, W5-D2, W5-D3 | high |
| W6-VB | Run validate_budget.py assertion | build | W4-B7, W4-0 | high |
| W6-VC | Verifier: run all scripts, cross-check specs.yaml | build | W5-C1, W5-C2, W5-C3, W5-C4, W4-B1, W4-B2, W4-B3, W4-B4, W4-B5, W4-B6, W4-B7 | high |
| W7 | Final analyst report | dp-analyst | W6-VA, W6-VB, W6-VC | medium |

---

## WAVE 0 — Foundation (All 4 in parallel)

### Task W0-A: Read POR snapshot + extract summary

```
TaskCreate:
  subject: "Read POR snapshot and write consolidated summary"
  description: |
    Read {OUT_DIR}/snapshot_por.md (pre-generated by tools/snapshot_por.py).
    This single file contains ALL POR specs, tables, budget lines, and investigation
    verdicts — do NOT read individual POR files.

    Write a consolidated summary to {OUT_DIR}/tmp_por_summary.md covering:

    - Room dimensions and partition layout
    - All speaker models, sensitivity, impedance per channel
    - Sub driver specs as stated (flag if Vas=15.5 or Qts=0.36 — these are WRONG)
    - Sub cabinet dimensions as stated in each doc (note conflicts)
    - Amplifier specs per channel
    - SPL targets
    - Every budget line item with stated totals (Phase 1 and Phase 2)
    - Electrical feed spec (correct: #2 Al, 240V, 60A breaker at main panel)
    - HVAC: MrCool system at $2,359
    - Projector model and throw distance
    - Screen specs (size, type, bottom height)
    - DSP platform as stated in each doc
    - Flag any doc that contradicts another doc

    DO NOT read any team_test*/ folder or individual POR files.
    Be TERSE — tables, not prose. Target <12KB output.
  metadata: {"agent_type": "build", "priority": "high"}
```

### Task W0-B: Read investigations + OBJ skill

```
TaskCreate:
  subject: "Read investigation verdicts and OBJ model skill"
  description: |
    Read ALL of the following and write consolidated notes to
    {OUT_DIR}/tmp_investigation_notes.md:

    - D:/Projects/Claude/investigations/ — all *.md files (read all, note verdict for each)
    - D:/Projects/Claude/theater/POR/skills/obj-model-generation/SKILL.md
    - D:/Projects/Claude/theater/screenshots/layout_config.json

    Notes must include:
    - For each investigation: topic, verdict, date, actionable conclusions
    - DSP platform verdict: APM2 (ADAU1701), NOT ADAU1452. Note PCM5102 DAC fix.
    - Signal chain: X6800H → APM2 ADC → ADAU1701 → I2S → PCM5102 → ICEpower
    - Internal DAC limit: 0.9 Vrms (too low). PCM5102 path: 2.0 Vrms → 190W/ch.
    - OBJ skill: winding convention, vertex caching, circular cutout method
    - layout_config.json: extract sub cabinet footprint (expect: 24"×24")
    - layout_config.json: extract all speaker positions, row positions, stage dims

    DO NOT read any team_test*/ folder.
  metadata: {"agent_type": "build", "priority": "high"}
```

### Task W0-C: Generate coordinates.json

```
TaskCreate:
  subject: "Generate coordinates.json locked artifact"
  description: |
    Generate the file {OUT_DIR}/coordinates.json from the EXACT coordinate table
    in .claude/skills/design-package.md (the table under "CRITICAL: EXACT COORDINATES").

    This file is the ONLY permitted coordinate source for all model generators.
    Do NOT read team_test*/ to find this table. Read the skill file directly.

    Required content — write exactly this structure (values must match the table):

    {
      "_source": "design-package.md coordinate table + layout_config.json",
      "_validated": "2026-03-20",
      "note": "ALL values in inches. Scale x2.54 for cm OBJ output.",
      "coordinate_system": {
        "X": "West to East, 0=west exterior wall",
        "Y": "Floor to Ceiling, 0=floor",
        "Z": "South to North, 0=south exterior wall",
        "units": "inches"
      },
      "building": {"X1": 0, "X2": 348, "Z1": 0, "Z2": 276},
      "walls": {
        "north":     {"X1": 0,   "X2": 348, "Z1": 270, "Z2": 276, "Y1": 0, "Y2": 120},
        "south":     {"X1": 0,   "X2": 348, "Z1": 0,   "Z2": 6,   "Y1": 0, "Y2": 120, "has_door": false},
        "east":      {"X1": 342, "X2": 348, "Z1": 0,   "Z2": 276, "Y1": 0, "Y2": 120, "has_door": true},
        "partition": {"X1": 138, "X2": 144, "Z1": 0,   "Z2": 276, "Y1": 0, "Y2": 120}
      },
      "door": {
        "wall": "east",
        "X1": 342, "X2": 348, "Z1": 198, "Z2": 234, "Y1": 0, "Y2": 80,
        "_assertion": "door_X must be near 342-348, NOT near 0-6. East wall = high X."
      },
      "equipment_closet": {
        "side": "gym (west of partition)",
        "X1": 90, "X2": 138, "Z1": 228, "Z2": 276, "Y1": 0, "Y2": 120,
        "_assertion": "closet_X2 must be less than 144. Gym side = LOW X."
      },
      "screen": {
        "wall": "north",
        "X1": 172, "X2": 320, "Z1": 248, "Z2": 249, "Y1": 30, "Y2": 113,
        "_assertion": "screen_Z must be near 248-276. North wall = HIGH Z. Bottom at Y=30 not 51."
      },
      "stage": {"X1": 144, "X2": 348, "Z1": 250, "Z2": 276, "Y1": 0, "Y2": 27},
      "corner_subs": {
        "_footprint_resolved": "24x24 inches (layout_config.json overrides specs.yaml 20x20)",
        "NW": {"X1": 144, "X2": 168, "Z1": 252, "Z2": 276, "Y1": 0, "Y2": 72},
        "NE": {"X1": 324, "X2": 348, "Z1": 252, "Z2": 276, "Y1": 0, "Y2": 72},
        "SW": {"X1": 144, "X2": 168, "Z1": 0,   "Z2": 24,  "Y1": 0, "Y2": 72},
        "SE": {"X1": 324, "X2": 348, "Z1": 0,   "Z2": 24,  "Y1": 0, "Y2": 72}
      },
      "riser":        {"X1": 156, "X2": 336, "Z1": 90,  "Z2": 156, "Y1": 0,  "Y2": 24},
      "row1_seating": {"X1": 198, "X2": 294, "Z1": 132, "Z2": 168, "Y1": 0,  "Y2": 18},
      "row2_seating": {"X1": 198, "X2": 294, "Z1": 96,  "Z2": 132, "Y1": 24, "Y2": 42},
      "projector":    {"X1": 236, "X2": 256, "Z1": 60,  "Z2": 276, "Y1": 110, "Y2": 120},
      "equipment_rack": {"X1": 102, "X2": 126, "Z1": 240, "Z2": 270, "Y1": 0, "Y2": 78},
      "power_panel":  {"X1": 342, "X2": 348, "Z1": 226, "Z2": 234, "Y1": 36, "Y2": 60},
      "speaker_dims": {
        "titan_815lx": {"W_in": 19.5, "H_in": 42.5, "D_in": 21.5, "units": "in", "source": "POR/titan_815lx/diagrams/titan_815lx.obj bounding box"},
        "volt_10":     {"W_in": 14,   "H_in": 14,   "D_in": 9.4,  "units": "in", "source": "POR/volt10/models/volt10_standard.obj bounding box (model in cm)"}
      }
    }

    After writing the file, read it back and verify the door X1 is 342, the closet X2 is 138,
    and the screen Z1 is 248. If any value is wrong, fix and re-read.
  metadata: {"agent_type": "build", "priority": "high"}
```

### Task W0-D: Generate ts_params.json

```
TaskCreate:
  subject: "Generate ts_params.json from verified Dayton datasheet values"
  description: |
    Generate the file {OUT_DIR}/ts_params.json with the authoritative T/S parameters
    for the Dayton Audio UMII18-22 driver.

    SOURCE: Dayton Audio UMII18-22 datasheet / Parts Express SKU 295-487.
    DO NOT copy values from POR docs — they contain INCORRECT values (Vas=15.5, Qts=0.36).
    The correct values from the verified Dayton datasheet are:

    {
      "_source": "Dayton Audio UMII18-22 datasheet, Parts Express SKU 295-487",
      "_incorrect_values_in_por": "Vas=15.5 cu ft, Qts=0.36 — DO NOT USE",
      "UMII18_22": {
        "Fs_Hz": 22.0,
        "Qts": 0.53,
        "Qes": 0.58,
        "Qms": 6.22,
        "Vas_cuft": 8.77,
        "Xmax_mm": 28,
        "Re_ohm": 5.8,
        "BL_Tm": 21.5,
        "Sd_cm2": 1026,
        "Le_mH": 3.66,
        "sensitivity_1W1m_dB": 95.7,
        "power_handling_W": 1500
      },
      "sealed_cabinet_calcs": {
        "footprint_in": "24x24 (resolved from layout_config.json)",
        "heights_to_evaluate": [72, 96, 120],
        "note": "Fc, Qtc, F3 must be calculated for each height using Qts=0.53, Vas=8.77 cuft"
      }
    }

    Also calculate and append:
    For each cabinet height (72", 96", 120") with 24"x24" footprint:
    - Gross internal volume (cubic feet)
    - Net internal volume after bracing (assume 15% displacement)
    - Vb per driver (2 drivers per cabinet)
    - Qtc = Qts * sqrt(Vb/Vas + 1)
    - Fc = Fs * sqrt(Vb/Vas + 1)  [this formula is wrong — use: Fc = Fs * sqrt(1 + Vas/Vb)]

    CORRECT sealed box formulas:
    - alpha = Vas/Vb
    - Qtc = Qts * sqrt(alpha + 1)
    - Fc = Fs * sqrt(alpha + 1)
    - F3: solve numerically or use approximation F3 ≈ Fc * sqrt((1/Qtc^2 - 2 + sqrt((1/Qtc^2 - 2)^2 + 4))/2)

    Add a "team_recommendation" field with the recommended height and rationale.
    Target Qtc 0.7 for maximally flat, but 0.9-1.0 is acceptable given room gain at 3,910 cu ft.

    Write the completed file to {OUT_DIR}/ts_params.json.
  metadata: {"agent_type": "build", "priority": "high"}
```

---

## WAVE 1 — Audit (Both in parallel, blocked by all W0)

Both W1-A and W1-B are blocked by: W0-A, W0-B, W0-C, W0-D.
Dispatch BOTH W1-A and W1-B in a SINGLE message (2 Agent calls).

### Task W1-A: Architect audit

```
TaskCreate:
  subject: "Architect: design document tree, flag inconsistencies, plan scope"
  description: |
    You are the Architect. Read:
    - {OUT_DIR}/tmp_por_summary.md (W0-A output)
    - {OUT_DIR}/tmp_investigation_notes.md (W0-B output)
    - {OUT_DIR}/coordinates.json (W0-C output)
    - {OUT_DIR}/ts_params.json (W0-D output)

    DO NOT read any team_test*/ folder.

    Produce {OUT_DIR}/tmp_architect.md covering:

    1. DOCUMENT TREE REVIEW
       - Confirm the 7-doc structure (00_index through 06_budget) is correct
       - Note any doc that has scope overlap or missing coverage
       - Senior says: keep it to 6-7 docs, no more. Challenge any scope creep.

    2. INCONSISTENCY FLAGS
       - List every contradiction between POR docs and Option D spec
       - List every place T/S incorrect values (Vas=15.5) appear in source docs
       - List every place sub cabinet dims are ambiguous (20" vs 24")
       - Screen bottom height error: 51" in source, correct value is 30"
       - DSP platform: any doc still citing ADAU1452? Flag it.

    3. SETTLED vs CONTESTED decisions
       - Settled (no team deliberation needed): list
       - Contested (needs explicit critical assessment): list
       - Open (requires team calculation): sub cabinet height (72/96/120")

    4. INVESTIGATION TRIGGER CHECK
       If any open question requires new web research or verification:
       - Create a DAG task with agent_type "research" or "verify"
       - Wire it to block only the specific doc(s) that depend on the answer
       - Do NOT block all of Phase 4 for a single open question

    5. ADVISORY VOICE: SENIOR
       Senior's check: Is the document scope right-sized? Are any deliverables
       over-engineered for the actual audience and use case (owner reading for 2027 build)?

    6. ADVISORY VOICE: ACTON
       Acton's check: Every assertion in the Architect plan must cite a source doc or
       investigation verdict. No unsourced claims about the design state.

    Write findings to {OUT_DIR}/tmp_architect.md. Be terse. Tables over prose.
    Target <12KB output.
  metadata: {"agent_type": "dp-architect", "priority": "high"}
  addBlockedBy: [W0-A-id, W0-B-id, W0-C-id, W0-D-id]
```

### Task W1-B: Analyst baseline

```
TaskCreate:
  subject: "Analyst: budget baseline, SPL calcs, gap analysis"
  description: |
    You are the Analyst. Read:
    - {OUT_DIR}/tmp_por_summary.md (W0-A output)
    - {OUT_DIR}/tmp_investigation_notes.md (W0-B output)
    - {OUT_DIR}/ts_params.json (W0-D output — use these T/S values ONLY)

    DO NOT read any team_test*/ folder.

    Produce {OUT_DIR}/tmp_analyst.md covering:

    1. BUDGET BASELINE
       Re-sum EVERY budget line item from source docs using Python arithmetic.
       Do not trust stated totals — compute them.
       Format: table with item, stated amount, source doc, verified sum.
       Flag any line item that doesn't match the stated total.
       ADVISORY — COST CUTTER: Challenge every estimate over $500. Is it sourced?
       Is shipping included? Is installation labor included?

    2. SPL CALCULATIONS (show all work)
       For each channel type:
       - Sensitivity (source: spec sheet, cite it)
       - Amplifier power available
       - SPL at MLP = sensitivity + 10*log10(power) - 20*log10(distance)
       - Headroom to clipping
       Target: 95 dB all channels at MLP (-10 dB reference).

    3. SUB CABINET HEIGHT ASSESSMENT
       Using ts_params.json values (Qts=0.53, Vas=8.77 cuft, Fs=22 Hz):
       Calculate Fc, Qtc, F3 for heights 72", 96", 120" with 24"x24" footprint.
       Use ts_params.json — it has these pre-calculated. Verify the math.
       ADVISORY — BASS HEAD: Which height gives the best LF extension while
       maintaining Qtc < 1.0? Consider room gain at 3,910 cu ft.

    4. PROJECTOR THROW VERIFICATION
       JVC NZ500 throw ratio range. Room depth available after stage.
       Can it fill 170" 16:9 at available throw distance?
       Minimum required throw distance for 170" 16:9.

    5. GAIN STAGING
       X6800H sub pre-out: 2V RMS
       APM2 ADC input full-scale: 2V RMS (confirm no clipping)
       ADAU1701 → I2S → PCM5102: output 2.0V RMS
       ICEpower 1200AS2 sensitivity: 3.55V for 600W @ 8 ohm
       ICEpower 1200AS2 output at 2.0V input: calculate
       State dB headroom lost vs rated power.

    6. GAP LIST
       Any spec missing from source docs that must be addressed in build.

    Write findings to {OUT_DIR}/tmp_analyst.md. Show arithmetic. Tables over prose.
    SCOPE CONSTRAINT: Write ONLY tmp_analyst.md. Do NOT write Python scripts,
    calculators, or any additional files. All calculations go inline in the markdown.
    Target <15KB output.
    ADVISORY — ACTON: Every T/S value must cite ts_params.json (Dayton datasheet).
    Any value cited from POR docs gets flagged — POR has wrong T/S params.
  metadata: {"agent_type": "dp-analyst", "priority": "high"}
  addBlockedBy: [W0-A-id, W0-B-id, W0-C-id, W0-D-id]
```

---

## WAVE 2 — Critique (Sequential, blocked by both W1)

### Task W2: Critic review

```
TaskCreate:
  subject: "Critic: review architect + analyst, resolve conflicts, recommend proceed/adjust/stop"
  description: |
    You are the Critic. Read:
    - {OUT_DIR}/tmp_architect.md (W1-A output)
    - {OUT_DIR}/tmp_analyst.md (W1-B output)
    - {OUT_DIR}/ts_params.json (for T/S verification)

    DO NOT read any team_test*/ folder.
    DO NOT start with the Architect's framing — read both outputs independently first.

    Produce {OUT_DIR}/tmp_critic.md covering:

    1. CONFLICT RESOLUTION
       Any point where Architect and Analyst disagree: state both positions, state
       which is correct and why. Cite source.

    2. BUDGET ARITHMETIC VERDICT
       Did Analyst's re-sum match stated totals?
       If not: state the discrepancy amount, flag as BLOCKER.
       This is a hard gate — incorrect arithmetic must be resolved before Phase 4.
       ADVISORY — COST CUTTER: Are there any line items that look optimistic?
       ADVISORY — ACTON: Every total must come from summing line items, not from
       reading a stated figure.

    3. T/S PARAMETER VERIFICATION
       Are ALL sub calculations in the Analyst output sourced from ts_params.json?
       If any calculation cites Vas=15.5 or Qts=0.36: flag as BLOCKER.
       The correct values are Qts=0.53, Vas=8.77 cuft (from Dayton datasheet).

    4. SUB CABINET HEIGHT DECISION
       Based on Analyst's Fc/Qtc/F3 table, recommend one height.
       ADVISORY — BASS HEAD: Provide the LF extension argument for the recommended height.
       State: chosen height, Qtc, Fc, F3, rationale (room gain, visual, structural role).
       This decision MUST be made here, not deferred to Phase 4.

    5. SCOPE CHECK
       Is the 7-doc structure right-sized? Any doc over-scoped or under-scoped?
       ADVISORY — SENIOR: Challenge any scope that exceeds what the owner needs for a 2027 build.

    6. VERDICT
       State one of: PROCEED / ADJUST [specify what] / STOP [specify why]
       List any BLOCKER items that must be resolved before Phase 4.
       List any ADVISORY items that should be addressed but are not blockers.

    Write findings to {OUT_DIR}/tmp_critic.md.
  metadata: {"agent_type": "dp-critic", "priority": "high"}
  addBlockedBy: [W1-A-id, W1-B-id]
```

---

## WAVE 3 — Decision Gate (Main conversation handles this, no agent dispatch)

**The main conversation reads {OUT_DIR}/tmp_critic.md and synthesizes for the user.**

Present to user:
- Plan overview (confirmed deliverable list)
- Budget baseline and any discrepancies
- Sub cabinet height recommendation (Critic's decision)
- Critic verdict: PROCEED / ADJUST / STOP
- Any BLOCKER items

**Ask user:** "Proceed / Adjust [describe] / Stop?"

On PROCEED: mark the W3-GATE task complete, dispatch Wave 4.
On ADJUST: update the relevant tmp_* files (or create correction tasks), loop back to Critic.
On STOP: mark all downstream tasks deleted.

```
TaskCreate:
  subject: "Decision gate — await user approval before Phase 4"
  description: |
    Sentinel task. Main conversation presents Critic summary to user and awaits
    proceed/adjust/stop. Mark completed only when user says PROCEED.
    agent_type: gate — handled by main conversation, not a dispatched agent.
  metadata: {"agent_type": "gate", "priority": "high"}
  addBlockedBy: [W2-id]
```

---

## WAVE 4 — Build Wave A: specs.yaml + docs in parallel (max 8 agents)

All Wave 4 tasks blocked by W3-GATE.

Dispatch order (8-agent cap):
- W4-0 alone first (all others blocked by it), then fan out all 8 (W4-B1 through W4-B7 + W4-C0) in one batch

### Task W4-0: Generate specs.yaml

```
TaskCreate:
  subject: "Generate specs.yaml — single source of truth for all docs and diagrams"
  description: |
    Generate {OUT_DIR}/specs.yaml. This is the ONLY number source for all downstream
    doc and diagram agents. Every value must be traceable to a source doc or
    investigation verdict.

    Read:
    - {OUT_DIR}/tmp_analyst.md (verified numbers)
    - {OUT_DIR}/tmp_critic.md (resolved decisions, sub cabinet height, corrections)
    - {OUT_DIR}/coordinates.json (spatial layout — use for positions, not recalculate)
    - {OUT_DIR}/ts_params.json (T/S parameters — use verbatim, do not copy from POR)

    Required sections in specs.yaml:
    - room: dimensions, partition, theater zone, gym zone, volumes
    - hvac: model, price ($2,359), placement, noise spec
    - electrical: feed (#2 Al, 60A at main panel NOT 100A), subpanel, circuits
    - pool_loads: Pentair IntelliFlow VSF draw, heater, NEC calculated load (39.6A)
    - upgrade_path: 90A breaker swap ($30, no rewire)
    - speakers: all 13 channels — model, sensitivity, impedance, nominal power, SPL at MLP
    - subwoofers:
        drivers: UMII18_22 (8 corner, 4 nearfield deferred)
        corner_cabinet:
          footprint_in: 24x24  # resolved from layout_config.json
          height_in: [Critic's choice from tmp_critic.md]
          Qtc: [from ts_params.json calculation]
          Fc_Hz: [from ts_params.json calculation]
          F3_Hz: [from ts_params.json calculation]
        tactile: 8x Aura Pro AST-2B-4
    - amplification:
        sub_amps: 5x ICEpower 1200AS2, 240V
        lcr_amp: ATI AT525NC
        processor: Denon AVR-X6800H
    - dsp:
        platform: "2x Wondom APM2 (ADAU1701)"  # NOT ADAU1452
        power: "ICEpower 1200AS2 AUX5V output"
        dac: "PCM5102 external DAC module (NOT internal ADAU1701 DAC)"
        signal_chain: "X6800H → APM2 ADC → ADAU1701 → I2S → PCM5102 → ICEpower 1200AS2"
    - signal_voltages:
        x6800h_sub_pre_out_vrms: 2.0
        apm2_adc_fullscale_vrms: 2.0
        pcm5102_output_vrms: 2.0
        icepower_sensitivity_vrms: 3.55
        icepower_power_at_2vrms_W: [calculate: (2.0/3.55)^2 * 600]
    - screen:
        size_in: 170
        aspect: "16:9"
        type: "acoustically transparent"
        bottom_height_in: 30  # correction: source docs say 51, correct is 30
        top_height_in: 113
    - projector:
        model: "JVC NZ500 (or 2027 successor)"
        throw_ratio_min: [from spec]
        throw_ratio_max: [from spec]
        available_throw_ft: [calculate from room depth - stage depth]
    - budget:
        phase1_total: [from Analyst verified sum]
        phase2_total: [from Analyst verified sum]
        grand_total: [sum]
        deferred: ["nearfield subs", "description"]
    - corrections_applied: [list all known corrections from design-package.md skill]

    Write {OUT_DIR}/specs.yaml. No placeholder values — every field must have a real number
    or explicit "TBD: [reason]" with source doc cited.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W3-GATE-id]
```

When W4-0 completes, dispatch the next 5 in parallel:

### Task W4-B1: Write 00_index.md

```
TaskCreate:
  subject: "Write 00_index.md — master index, system summary, corrections log"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY. Do not re-read POR docs.
    Write {OUT_DIR}/00_index.md:

    - System summary table (key specs at a glance)
    - Document map (what each doc covers, line count)
    - Known corrections log (all corrections from specs.yaml corrections_applied list)
    - Team decisions (sub cabinet height resolution, DSP platform, footprint conflict resolution)
    - Revision status (current run date)

    Tables over prose. Terse. Cite specs.yaml fields for every number.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id]
```

### Task W4-B2: Write 01_room_electrical_hvac.md

```
TaskCreate:
  subject: "Write 01_room_electrical_hvac.md"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY.
    Write {OUT_DIR}/01_room_electrical_hvac.md:

    - Room dimensions, partition layout, theater zone, gym zone
    - Equipment closet location (LEFT/gym side, X=90-138, NOT theater side)
    - HVAC: MrCool DIY 12K on partition wall, $2,359, ~23 dB(A)
    - Electrical: #2 Al 240V with 60A breaker at main panel (NOT 100A — source docs wrong)
    - Dedicated subpanel spec
    - Pool loads: Pentair IntelliFlow VSF + heater, NEC calculated load 39.6A
    - Headroom on 60A: 34%
    - Upgrade path: 90A breaker swap at main panel ($30, no rewire)
    - Pool pump scheduling note

    Every number must cite specs.yaml. Show calculations for load analysis.
    ADVISORY — ACTON: Every electrical claim must have a code citation (NEC section)
    or a calculation shown inline.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id]
```

### Task W4-B3: Write 02_speakers.md

```
TaskCreate:
  subject: "Write 02_speakers.md"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY.
    Write {OUT_DIR}/02_speakers.md:

    - All 13 channels table: channel label, model, sensitivity (dB/W/m), impedance, amp, power (W), SPL at MLP
    - Titan-815LX: 6Ω nominal (NOT 8Ω), ported enclosure — line panels, don't stuff
    - Volt-10: 95 dB sensitivity (NOT 98 dB — source docs have error)
    - SPL calculation shown: SPL = sensitivity + 10*log10(power) - 20*log10(distance)
    - Headroom table per channel
    - Critical assessment: WHY all-Volt-10 surrounds (timbral match), WHY Titan for LCR
    - Known weaknesses of each choice

    ADVISORY — ACTON: Sensitivity figures must cite the DIYSG spec sheet, not project docs.
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-0-id]
```

### Task W4-B4: Write 03_subwoofers.md

```
TaskCreate:
  subject: "Write 03_subwoofers.md — sub system with verified T/S calculations"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY. Read {OUT_DIR}/ts_params.json for T/S verification.
    Write {OUT_DIR}/03_subwoofers.md:

    - Driver: UMII18-22 specs table (from ts_params.json — NOT from POR docs)
    - Flag: source docs have INCORRECT T/S values (Vas=15.5, Qts=0.36). Correct: Qts=0.53, Vas=8.77.
    - Corner sub system: 8 drivers in 4 cabinets (stacked rectangular)
    - Cabinet: 24"×24"×[chosen height]" sealed, 2 drivers per cabinet
    - Volume calculation: gross → net (15% displacement) → per driver
    - Sealed box calcs: show Qtc, Fc, F3 formulas with numbers (use ts_params.json values)
    - WHY sealed (not ported): easier EQ, no port noise, room gain compensates rolloff
    - Corner loading: +11 dB boundary gain
    - Acoustic summing from 4 corners: +~6 dB
    - Nearfield subs: deferred — note 4× UMII18-22 planned, add post-build
    - Tactile: 8× Aura Pro AST-2B-4 bass shakers
    - ICEpower 1200AS2 power into 8Ω (series wiring): [from specs.yaml]
    - Excursion limit: Xmax=28mm is binding below ~13Hz, not amp power
    - Critical assessment: WHY this sub configuration vs alternatives

    ADVISORY — BASS HEAD: Confirm the Qtc choice gives acceptable LF extension given room gain.
    State estimated in-room F3 accounting for room gain at 3,910 cu ft.
    ADVISORY — ACTON: Every calculation must show the formula, input values (sourced), and result.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id]
```

After W4-B1 through W4-B4 dispatch, dispatch next 5:

### Task W4-B5: Write 04_amplification_dsp.md

```
TaskCreate:
  subject: "Write 04_amplification_dsp.md"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY.
    Write {OUT_DIR}/04_amplification_dsp.md:

    - Amplification table: channel group, amp model, power, load, notes
    - ICEpower 1200AS2: 240V, fanless, no infrasonic derating (unlike NX6000D)
    - ATI AT525NC for LCR + front wides
    - DSP platform: 2× Wondom APM2 (ADAU1701) — NOT ADAU1452
    - APM2 power source: ICEpower 1200AS2 AUX5V output
    - Signal chain: X6800H sub pre-out (2V RMS) → APM2 ADC (2V full-scale) → ADAU1701 DSP
      → I2S digital out → PCM5102 external DAC (2.0V RMS) → ICEpower 1200AS2 input
    - WHY PCM5102, NOT internal ADAU1701 DAC:
      Internal DAC = 0.9 Vrms → ICEpower outputs only ~38.5W/ch
      PCM5102 = 2.0 Vrms → ~190W/ch (calculation shown)
    - Dirac ART: per-channel sub pre-outs preserved (no summing)
    - Gain staging table: every stage with voltage, headroom
    - Critical assessment: WHY APM2 (not miniDSP, not ADAU1452)

    ADVISORY — ACTON: Signal chain voltages must be calculated, not asserted.
    Show: V_out/V_sensitivity = power fraction, power = fraction^2 × rated_power.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id]
```

### Task W4-B6: Write 05_screen_seating.md

```
TaskCreate:
  subject: "Write 05_screen_seating.md"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY.
    Write {OUT_DIR}/05_screen_seating.md:

    - Screen: 170" 16:9 AT, centered on north wall, bottom at 30" (NOT 51")
    - Screen width and height in inches (calculate from diagonal + aspect ratio)
    - Projector: JVC NZ500 (or 2027 successor), throw ratio range
    - Available throw distance: room depth - stage depth (show calculation)
    - Throw ratio at available distance: throw_distance / screen_width
    - Is it within projector's throw range? State yes/no with numbers.
    - UST projectors: OFF THE TABLE — incompatible with AT screen (LCR behind screen)
    - Viewing angles: Row 1 and Row 2 horizontal and vertical angles to screen edges
    - Show geometry: seat eye height, screen center, angle = atan(Δy/Δz) or atan(Δx/Δz)
    - Riser height: 24", Row 2 eye height: ~24"+18"+eye = ~54"
    - Critical assessment: is the geometry workable? Any viewing angle concerns?
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-0-id]
```

### Task W4-B7: Write 06_budget.md

```
TaskCreate:
  subject: "Write 06_budget.md — verified budget with arithmetic"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY (budget section has verified sums).
    Write {OUT_DIR}/06_budget.md:

    - Phase 1 budget table: all line items, unit costs, quantities, totals
    - Phase 2 budget table: same format
    - Owned inventory table: what's already purchased
    - Deferral table: nearfield subs and other deferred items
    - Grand total with and without deferrals
    - Budget notes: corrections applied (MrCool at $2,359, etc.)

    Use Python in-line to verify: print the sum of all Phase 1 line items.
    If sum ≠ stated Phase 1 total: note the discrepancy and use the correct sum.
    Do the same for Phase 2.

    ADVISORY — COST CUTTER: Every line item over $500 must include source (URL or vendor)
    and note whether shipping is included. Flag any line item without a source.
    Tables only. No prose except for footnotes on specific items.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id]
```

### Task W4-C0: Generate diagrams/config.py

```
TaskCreate:
  subject: "Generate diagrams/config.py — shared config loaded from specs.yaml"
  description: |
    Read {OUT_DIR}/specs.yaml ONLY.
    Generate {OUT_DIR}/diagrams/config.py.

    This file is imported by all 4 diagram scripts (d01-d04). It must:
    - Load specs.yaml using PyYAML or parse it directly
    - Export all constants the diagram scripts need: room dims, speaker positions,
      sub positions, seat positions, stage dims, screen dims
    - Be importable with zero errors: `from config import *`
    - Include a __version__ field matching the run

    Also generate {OUT_DIR}/diagrams/generate_all.py:
    - Imports and runs d01, d02, d03, d04 scripts in sequence
    - Outputs PNGs to {OUT_DIR}/diagrams/diagrams_output/
    - Handles errors per script (log and continue, don't abort all)
    - Run: `python generate_all.py` from the diagrams/ directory

    Test: from the diagrams/ directory, run `python -c "from config import *; print('OK')"`.
    If it fails, fix before marking complete.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id]
```

---

## WAVE 5 — Build Wave B: Diagrams + Models (parallel, max 8 agents)

Dispatch in two groups:
- Group 1: W5-C1, W5-C2, W5-C3, W5-C4 (blocked by W4-C0)
- Group 2: W5-D1, W5-D2, W5-D3 (blocked by W4-0 + W0-C)

Dispatch D2 (hires model) first — it is the slowest task.

### Task W5-C1: Generate d01_floor_plan.py

```
TaskCreate:
  subject: "Generate d01_floor_plan.py — top-down floor plan"
  description: |
    Read {OUT_DIR}/diagrams/config.py and {OUT_DIR}/specs.yaml.
    Generate {OUT_DIR}/diagrams/d01_floor_plan.py:

    Top-down view (X-Z plane, Y ignored):
    - Room outline: theater zone (144-348 X, 0-276 Z) + gym zone (0-144 X)
    - Partition wall at X=138-144
    - East wall door opening at X=342-348, Z=198-234
    - Equipment closet outline at X=90-138, Z=228-276 (gym side, LEFT)
    - Stage at Z=250-276
    - Screen line at Z=248-249 (north wall)
    - Corner subs: NW, NE, SW, SE boxes at correct coordinates from config.py
    - Riser outline
    - Seating rows (Row 1 floor level, Row 2 on riser)
    - Speaker positions (circles or labeled boxes)
    - Labels for all elements

    Must run with: `cd diagrams && python d01_floor_plan.py`
    Output: {OUT_DIR}/diagrams/diagrams_output/d01_floor_plan.png
    Use matplotlib only. No external dependencies.
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-C0-id]
```

### Task W5-C2: Generate d02_signal_flow.py

```
TaskCreate:
  subject: "Generate d02_signal_flow.py — full signal path"
  description: |
    Read {OUT_DIR}/diagrams/config.py and {OUT_DIR}/specs.yaml.
    Generate {OUT_DIR}/diagrams/d02_signal_flow.py:

    Full signal chain diagram (block diagram style):
    - Sources → Denon X6800H → channel amps
    - Sub path: X6800H sub pre-out → APM2 ADC → ADAU1701 DSP → I2S → PCM5102 DAC
      → ICEpower 1200AS2 → corner subs (4 per amp, series 8Ω)
    - LCR path: X6800H → ATI AT525NC → Titan-815LX (L/C/R)
    - Surround/Atmos path: X6800H internal → Volt-10 (all 10 channels)
    - Label signal levels at key points: 2.0V RMS, 2.0V RMS, 190W/ch
    - Include Dirac Live + ART in the chain

    Must run without error. Output PNG to diagrams_output/.
    Use matplotlib or graphviz. If graphviz: include fallback matplotlib version.
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-C0-id]
```

### Task W5-C3: Generate d03_front_elevation.py

```
TaskCreate:
  subject: "Generate d03_front_elevation.py — screen wall elevation"
  description: |
    Read {OUT_DIR}/diagrams/config.py and {OUT_DIR}/specs.yaml.
    Generate {OUT_DIR}/diagrams/d03_front_elevation.py:

    Front elevation view (X-Y plane, looking north from audience toward screen):
    - Room width 17' (144 to 348 inches X)
    - Ceiling at 120"
    - Floor at 0"
    - Stage deck 27" high, full width
    - Screen: 170" 16:9, centered, bottom at 30", top at 113"
    - LCR speakers on stage (Titan-815LX boxes, correct dims from config.py)
    - Front corner subs: NW at X=144-168, NE at X=324-348, both 72" tall
    - Front height Volt-10 speakers at ceiling (if applicable)
    - Measurements annotated: screen height, screen bottom, stage height

    Output PNG to diagrams_output/. matplotlib only.
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-C0-id]
```

### Task W5-C4: Generate d04_wiring.py

```
TaskCreate:
  subject: "Generate d04_wiring.py — sub system wiring diagram"
  description: |
    Read {OUT_DIR}/diagrams/config.py and {OUT_DIR}/specs.yaml.
    Generate {OUT_DIR}/diagrams/d04_wiring.py:

    Sub system wiring diagram:
    - Denon X6800H: 4 sub pre-outs (or however many per spec)
    - APM2 units: show 2× APM2, each with 4 outputs
    - ICEpower 1200AS2 units: show 5× units, stereo each = 10 channels
    - Driver wiring: each cabinet has 2× UMII18-22 in series (8Ω load)
    - 240V power to each ICEpower unit
    - AUX5V from ICEpower to APM2 power

    Show channel independence (Dirac ART requirement — no summing).
    Output PNG to diagrams_output/. matplotlib only.
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-C0-id]
```

### Task W5-D1: Generate generate_room.py + room_model.obj

```
TaskCreate:
  subject: "Generate generate_room.py — low-poly room OBJ model"
  description: |
    Read {OUT_DIR}/coordinates.json FIRST. This is the ONLY permitted coordinate source.
    Do NOT re-read POR docs for coordinates. Do NOT re-derive positions from English.
    Read D:/Projects/Claude/theater/POR/skills/obj-model-generation/SKILL.md for conventions.
    Read {OUT_DIR}/specs.yaml for speaker dims.

    Generate {OUT_DIR}/models/generate_room.py which, when run, produces
    {OUT_DIR}/models/room_model.obj.

    Model contents (all coordinates from coordinates.json):
    - Room shell: north wall, south wall, east wall (with door hole), partition wall
    - Door opening: cut from east wall using coordinates.json["door"] — X1=342, X2=348, Z1=198, Z2=234
    - Equipment closet: box at coordinates.json["equipment_closet"] — X1=90, X2=138 (LOW X, gym side)
    - Stage deck, riser, Row 1 seating, Row 2 seating (all from coordinates.json)
    - Screen: thin box at coordinates.json["screen"] — Z near 248-249, Y bottom=30
    - Projector: box at coordinates.json["projector"]
    - Equipment rack: box at coordinates.json["equipment_rack"]
    - Corner subs: 4 boxes at coordinates.json["corner_subs"]["NW/NE/SW/SE"]
    - Speaker boxes at positions from specs.yaml (use speaker_dims from coordinates.json)

    COORDINATE ENFORCEMENT:
    The model generator MUST read coordinates from coordinates.json at runtime:
      import json, pathlib
      COORDS = json.loads(pathlib.Path("../coordinates.json").read_text())
      door = COORDS["door"]  # X1=342, X2=348

    Do NOT hardcode coordinate values. Do NOT re-derive from English.

    OBJ conventions (from SKILL.md):
    - Winding: counter-clockwise when viewed from outside
    - Scale: inches × 2.54 = cm output
    - Vertex caching: build vertex list, then faces by index
    - Walls: 6" thick boxes
    - Transparent walls: use MTL with d=0.3

    Run the script after generating: `cd {OUT_DIR}/models && python generate_room.py`
    If it errors, fix and re-run. Do not mark complete if the script fails.

    After successful run, read the first 50 lines of room_model.obj to verify vertices exist.
    Report the door X range found in the OBJ as a TaskUpdate note.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-0-id, W0-C-id]
```

### Task W5-D2: Generate generate_room_hires.py + room_model_hires.obj

```
TaskCreate:
  subject: "Generate generate_room_hires.py — hi-res room OBJ with embedded speaker models"
  description: |
    Read {OUT_DIR}/coordinates.json FIRST — only permitted coordinate source.
    Read D:/Projects/Claude/theater/POR/skills/obj-model-generation/SKILL.md for conventions.
    Read {OUT_DIR}/specs.yaml for speaker positions and orientations.

    Generate {OUT_DIR}/models/generate_room_hires.py which produces room_model_hires.obj.

    Same room shell as low-poly (use coordinates.json), PLUS embed these external OBJ models:
    - 3× D:/Projects/Claude/theater/POR/titan_815lx/diagrams/titan_815lx.obj
      (LCR on stage, model in INCHES → multiply vertices by 2.54 for cm)
      Orientation: face south (toward audience). Rotate 180° around Y axis if default faces north.
    - 10× D:/Projects/Claude/theater/POR/volt10/models/volt10_standard.obj
      (surround/Atmos, model already in CM — no unit conversion)
      Wall Volt-10: face into room. FWL faces east, FWR faces west, SL east, SR west, SBL/SBR north.
      Ceiling Volt-10 (TFL/TFR/TRL/TRR): woofer faces DOWN (-Y), baffle faces floor.
    - 8× D:/Projects/Claude/theater/POR/umii18_22/diagrams/umii18_22.obj
      (2 per corner sub cabinet, model in CM — no conversion)
      Orientation: baffle faces south (toward audience, low Z).

    Embedding pattern (from SKILL.md):
    - Read source OBJ, parse vertex and face lines
    - Offset all vertex indices by current_vertex_count
    - Apply translation to correct room position (in cm)
    - Apply rotation matrix (Y-axis or as needed per speaker)
    - Prefix material names: titan_L_, titan_C_, titan_R_, volt_SL_, volt_SR_, etc.

    COORDINATE ENFORCEMENT: All room positions from coordinates.json. Do NOT re-derive.

    Run the script: `cd {OUT_DIR}/models && python generate_room_hires.py`
    If it errors, fix and re-run. Expected output: ~100K+ vertices, ~5MB OBJ.
    Report vertex count as TaskUpdate note.
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-0-id, W0-C-id]
```

### Task W5-D3: Generate generate_sub_cabinet.py + sub_cabinet.obj

```
TaskCreate:
  subject: "Generate generate_sub_cabinet.py — detailed sub cabinet OBJ"
  description: |
    Read {OUT_DIR}/ts_params.json for cabinet dimensions.
    Read {OUT_DIR}/coordinates.json for the resolved footprint (24"×24").
    Read D:/Projects/Claude/theater/POR/skills/obj-model-generation/SKILL.md for circular cutout method.

    Generate {OUT_DIR}/models/generate_sub_cabinet.py which produces sub_cabinet.obj.

    Cabinet spec:
    - Footprint: 24"×24" (from coordinates.json corner_subs footprint, NOT 20"×20")
    - Height: [from ts_params.json team_recommendation.height_in]
    - Sealed (no ports)
    - Panel thickness: 0.75" (3/4" MDF)
    - Internal partition at height/2 (separating 2 driver chambers)
    - Two 18" circular driver cutouts on front baffle
      - Use corner-fan method from SKILL.md (32-segment rings)
      - Cutout center positions: 1/4 and 3/4 of height
    - Internal bracing: one horizontal brace per chamber
    - Material: MTL with wood grain properties

    Scale: inches × 2.54 = cm output.

    Run the script: `cd {OUT_DIR}/models && python generate_sub_cabinet.py`
    If it errors, fix. Verify two circular cutouts exist in the OBJ output (search for arc vertices).
  metadata: {"agent_type": "build", "priority": "medium"}
  addBlockedBy: [W4-0-id, W0-C-id]
```

---

## WAVE 6 — Validate (All 3 in parallel, blocked by their respective upstream)

### Task W6-VA: Run validate_geometry.py assertions

```
TaskCreate:
  subject: "Validate OBJ placement — run geometry assertions on all 3 models"
  description: |
    Generate and run {OUT_DIR}/validate_geometry.py against the 3 OBJ outputs.
    This is a BLOCKING gate — if any assertion fails, this task fails and must be
    reported as BLOCKER before Wave 7 can run.

    Write validate_geometry.py:

    import sys, pathlib, json

    def load_vertices(obj_path):
        verts = []
        for line in pathlib.Path(obj_path).read_text().splitlines():
            if line.startswith('v '):
                parts = line.split()
                verts.append((float(parts[1]), float(parts[2]), float(parts[3])))
        return verts

    def check(label, verts, axis, lo, hi, tol=6*2.54):
        # axis: 0=X, 1=Y, 2=Z. Values in cm (inches * 2.54)
        hits = [v for v in verts if lo-tol <= v[axis] <= hi+tol]
        ok = len(hits) > 0
        status = "PASS" if ok else "FAIL"
        print(f"{status}: {label} — {len(hits)} vertices near {axis}=[{lo:.0f},{hi:.0f}]cm")
        return ok

    results = []
    for model_name in ["room_model.obj", "room_model_hires.obj"]:
        p = pathlib.Path("{OUT_DIR}/models") / model_name
        if not p.exists():
            print(f"SKIP: {model_name} not found")
            continue
        v = load_vertices(p)
        # Door: must have vertices at X near 342-348 inches = 869-884 cm
        results.append(check(f"{model_name} door (east wall X)", v, 0, 342*2.54, 348*2.54))
        # Closet: must have vertices at X < 144 inches = 366 cm
        closet_verts = [vert for vert in v if vert[0] < 144*2.54 and vert[2] > 228*2.54]
        ok = len(closet_verts) > 10
        print(f"{'PASS' if ok else 'FAIL'}: {model_name} closet (gym side low X) — {len(closet_verts)} verts")
        results.append(ok)
        # Screen: must have vertices at Z near 248-276 inches = 630-701 cm (north wall)
        results.append(check(f"{model_name} screen (north wall Z)", v, 2, 248*2.54, 276*2.54))

    all_pass = all(results)
    print(f"\n{'ALL PASS' if all_pass else 'FAILURES DETECTED — BLOCKER'}")
    sys.exit(0 if all_pass else 1)

    Run: `cd {OUT_DIR} && python validate_geometry.py`

    If exit code is non-zero:
    1. Report each FAIL line
    2. Mark this task as the blocking reason
    3. Create a repair task (agent_type: "build") for each failing model
    4. Wire repair tasks to block W7

    If all pass: mark this task complete.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W5-D1-id, W5-D2-id, W5-D3-id]
```

### Task W6-VB: Run validate_budget.py assertion

```
TaskCreate:
  subject: "Validate budget arithmetic — programmatic sum verification"
  description: |
    Generate and run {OUT_DIR}/validate_budget.py.
    This is a BLOCKING gate — if arithmetic fails, report as BLOCKER.

    The script reads {OUT_DIR}/specs.yaml, extracts all Phase 1 line items,
    sums them in Python, and compares to the stated Phase 1 total.
    Repeats for Phase 2. Reports PASS/FAIL for each.

    Write the script using the actual line item structure from specs.yaml.
    If specs.yaml has budget as a flat total without line items, read 06_budget.md instead
    and parse the markdown table numerically (strip $ signs, convert to float, sum).

    Acceptable tolerance: $0 (exact match required, since these are discrete line items).

    If FAIL: report the discrepancy amount and which items don't add up.
    Mark this task failed if any total doesn't match. Create a repair task if needed.

    Run: `cd {OUT_DIR} && python validate_budget.py`
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W4-B7-id, W4-0-id]
```

### Task W6-VC: Verifier — run all scripts, cross-check specs.yaml

```
TaskCreate:
  subject: "Verifier: run all diagram scripts, cross-check specs.yaml against source"
  description: |
    You are the Verifier. Run and check — report PASS/FAIL for each item.

    1. RUN ALL DIAGRAM SCRIPTS
       `cd {OUT_DIR}/diagrams && python generate_all.py`
       Report: which scripts produced PNGs, which errored. Fix errors or flag as BLOCKER.

    2. CROSS-CHECK specs.yaml VALUES (check 12 key values):
       For each value below, verify specs.yaml matches the authoritative source:
       - Room: 17'×23'×10' (theater zone only) — cite POR/01 or CLAUDE.md
       - Volt-10 sensitivity: 95 dB (NOT 98) — cite DIYSG spec
       - Titan-815LX impedance: 6Ω nominal (NOT 8Ω) — cite DIYSG spec
       - ICEpower 1200AS2: 240V, no infrasonic derating — cite investigation verdict
       - UMII18-22 Qts: 0.53 (NOT 0.36) — cite ts_params.json (Dayton datasheet)
       - UMII18-22 Vas: 8.77 cuft (NOT 15.5) — cite ts_params.json
       - Screen bottom: 30" (NOT 51") — cite corrections list
       - Electrical feed: #2 Al, 60A at main panel (NOT 100A) — cite NEC or POR correction
       - MrCool price: $2,359 — cite POR doc
       - DSP platform: APM2 (ADAU1701), NOT ADAU1452 — cite investigation verdict
       - PCM5102 DAC in signal chain (NOT internal ADAU1701 DAC) — cite investigation verdict
       - Sub footprint: 24"×24" (NOT 20"×20") — cite layout_config.json

    3. SIGNAL CHAIN VERIFICATION
       Verify specs.yaml signal_chain matches: "X6800H → APM2 ADC → ADAU1701 → I2S → PCM5102 → ICEpower 1200AS2"

    4. FILES OUTSIDE OUT_DIR CHECK
       Verify no files outside {OUT_DIR} were created or modified.
       Run: `find D:/Projects/Claude/theater -newer {OUT_DIR}/specs.yaml -not -path '{OUT_DIR}/*'`
       If any files found outside OUT_DIR: flag as BLOCKER.

    5. ADVISORY — ACTON: Every cross-check must cite the source doc and line/field.
       "specs.yaml says X — source doc says Y — match/mismatch" format.
    6. ADVISORY — SENIOR: Is the verification covering the critical path?
       Have the 3 most common failure modes been checked? (Door placement, closet placement, T/S params)

    Report: VERIFIED (all pass) or ISSUES FOUND (list each FAIL with source discrepancy).
    Write report to {OUT_DIR}/tmp_verifier.md.
  metadata: {"agent_type": "build", "priority": "high"}
  addBlockedBy: [W5-C1-id, W5-C2-id, W5-C3-id, W5-C4-id, W4-B1-id, W4-B2-id, W4-B3-id, W4-B4-id, W4-B5-id, W4-B6-id, W4-B7-id]
```

---

## WAVE 7 — Final Report (blocked by all W6)

### Task W7: Final analyst report

```
TaskCreate:
  subject: "Final analyst: budget delta, corrections applied, team summary"
  description: |
    Read:
    - {OUT_DIR}/tmp_verifier.md
    - {OUT_DIR}/validate_geometry.py output (if written to file)
    - {OUT_DIR}/validate_budget.py output (if written to file)
    - {OUT_DIR}/specs.yaml (for budget totals)
    - {OUT_DIR}/tmp_analyst.md (baseline from Phase 1)

    Produce {OUT_DIR}/00_final_report.md:

    1. BUILD SUMMARY
       Files generated: list all with sizes
       Scripts runnable: yes/no per script
       OBJ models: vertex counts, file sizes

    2. VERIFICATION VERDICT
       Geometry assertions: PASS/FAIL
       Budget arithmetic: PASS/FAIL
       specs.yaml cross-check: PASS count / 12, any failures
       Diagram scripts: PASS/FAIL

    3. CORRECTIONS APPLIED
       List every correction from the known corrections list.
       For each: what was wrong in source, what was written in output.

    4. BUDGET IMPACT
       Phase 1 total (corrected vs source)
       Phase 2 total (corrected vs source)
       Grand total

    5. OPEN ITEMS
       Any BLOCKER that was not resolved.
       Any advisory item that was not addressed.

    6. TEAM PERFORMANCE NOTE
       Sub cabinet height decision: was it made in Phase 2 (correct) or deferred (wrong)?
       T/S parameters: sourced from Dayton datasheet (correct) or copied from POR (wrong)?
       Coordinate placement: all 3 assertions passed on first run (correct) or required repair?

    Write terse tables. This is the final deliverable — the main conversation presents this to the user.
  metadata: {"agent_type": "dp-analyst", "priority": "medium"}
  addBlockedBy: [W6-VA-id, W6-VB-id, W6-VC-id]
```

---

## Execution Instructions for Main Conversation

### Pre-Run Setup (CRITICAL — do this BEFORE creating any tasks)

1. Determine OUT_DIR (next team_test_N increment).
2. **Pre-warm deferred tools:** Call `ToolSearch("select:TaskCreate,TaskUpdate,TaskGet,TaskList")` ONCE to fetch all task tool schemas. This avoids 3-5s delays later.
3. Create the output directory: `mkdir -p {OUT_DIR}/{diagrams/diagrams_output,models}`
4. **Generate POR snapshot:** Run `python -m tools.snapshot_por --out {OUT_DIR}/snapshot_por.md` — this pre-extracts all POR specs, tables, and investigation verdicts into one file. W0-A reads ONLY this file instead of 15+ individual POR docs.

### PARALLEL DISPATCH RULE (CRITICAL — most important optimization)

**All multi-agent waves MUST use `run_in_background: true` on every Agent call.** The Claude Code harness executes foreground Agent calls serially — even if you put multiple Agent blocks in one message, they run one at a time. Background agents run concurrently (up to 10 simultaneous).

For every wave with 2+ agents, dispatch each agent with `run_in_background: true`. You will be notified when each completes. Wait for ALL agents in the wave to complete before proceeding to the next wave.

Example — dispatching W1 (2 agents):
```
Agent(description="W1-A: Architect audit", subagent_type="dp-architect", run_in_background=true, prompt="...")
Agent(description="W1-B: Analyst baseline", subagent_type="dp-analyst", run_in_background=true, prompt="...")
```

Both launch concurrently. You get notified as each finishes. When ALL are done, proceed to the next wave.

This applies to ALL multi-agent waves:
- W0: 4 background Agent calls
- W1: 2 background Agent calls
- W4-B: up to 8 background Agent calls (note: max 10 concurrent)
- W5: up to 7 background Agent calls
- W6: 3 background Agent calls

Single-agent waves (W2, W4-0, W7) should use foreground (default) since there's nothing else to do while waiting.

### Dispatch Sequence (with explicit subagent_type)

1. Create all W0 tasks (4 TaskCreate calls in one message). Then dispatch all 4 W0 agents — all use `subagent_type: "build"`.
2. When ALL W0 complete: create W1-A and W1-B (2 TaskCreate calls). Then dispatch BOTH in ONE message:
   - W1-A uses `subagent_type: "dp-architect"`
   - W1-B uses `subagent_type: "dp-analyst"`
3. When both W1 complete: create and dispatch W2 using `subagent_type: "dp-critic"`.
4. When W2 complete: create W3-GATE. **Do not dispatch.** Present critic summary to user. Wait.
5. On user PROCEED: mark W3-GATE completed. Create W4-0. Dispatch alone (`subagent_type: "build"`).
6. When W4-0 complete: create W4-B1 through W4-B7 and W4-C0 (8 tasks). Dispatch all 8 in ONE message (all `subagent_type: "build"`).
7. When W4-C0 complete: create W5-C1 through W5-C4. Dispatch all 4 in ONE message. Also dispatch W5-D1, W5-D2, W5-D3 if W4-0 is already done (up to 7 Agent calls in ONE message). All `subagent_type: "build"`.
8. When all W5 complete: create W6-VA, W6-VB, W6-VC. Dispatch all 3 in ONE message (all `subagent_type: "build"`).
9. If W6-VA fails: create repair tasks. Wire them to block W7.
10. When all W6 complete (all pass): create W7. Dispatch (`subagent_type: "dp-analyst"`).
11. When W7 complete: present {OUT_DIR}/00_final_report.md to user.

### Agent Scope Constraints (applies to ALL agents)

- Write ONLY the files specified in your task description. Do NOT create bonus files (Python scripts, calculators, helper tools).
- **W0-D (ts_params.json):** Write ONLY ts_params.json. Do NOT write a Python calculation script. Show arithmetic in JSON comments or a `_calculations` field inside the JSON.
- **W1-B (Analyst):** Write ONLY tmp_analyst.md. Do NOT write Python scripts. All arithmetic goes inline in the markdown.
- **All agents:** If your output exceeds the target size in your task description, you are being too verbose. Consolidate.

---

## Source Restriction (applies to ALL agents)

**DO NOT read, reference, or copy from any `team_test*/` folder.**
These are prior run outputs. Any agent that reads them will corrupt the current run with stale data.
Source documents only: POR/, investigations/, CLAUDE.md, theater/CLAUDE.md, layout_config.json.
